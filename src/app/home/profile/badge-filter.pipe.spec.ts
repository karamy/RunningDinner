import { BadgeFilterPipe } from './badge-filter.pipe';

describe('BadgePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BadgeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
