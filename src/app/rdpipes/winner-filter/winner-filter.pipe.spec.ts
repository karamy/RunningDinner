import { WinnerFilterPipe } from './winner-filter.pipe';

describe('WinnerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WinnerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
