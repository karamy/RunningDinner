import { DinnerFilterPipe } from './dinner-filter.pipe';

describe('DinnerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DinnerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
